<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/scale_wheel_wrapper"
    android:id="@+id/layout_scale_wheel" android:visibility="gone">
    <TextView style="@style/scale_wheel_label" android:id="@+id/label_scale_wheel" android:text="100%"
              android:textColor="@android:color/white"/>
    <com.uc.android.widget.HorizontalProgressWheelView
        style="@style/scale_wheel" android:id="@+id/scale_wheel"/>

</LinearLayout>